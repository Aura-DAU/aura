# Copy to .env.local and fill in values. Never commit .env.local.

# Base URL of the Python RAG backend (FastAPI). Defaults to the local
# dev server if unset — MUST be set in deployed environments.
BACKEND_URL=http://127.0.0.1:8000
